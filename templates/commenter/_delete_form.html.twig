<form method="post" action="{{ path('app_commenter_delete', {'id': commenter.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ commenter.id) }}">
    <button class="btn">Delete</button>
</form>

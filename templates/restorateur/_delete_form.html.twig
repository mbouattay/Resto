<form method="post" action="{{ path('app_restorateur_delete', {'id': restorateur.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ restorateur.id) }}">
    <button class="btn">Delete</button>
</form>

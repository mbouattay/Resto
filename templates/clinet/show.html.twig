{% extends 'base.html.twig' %}

{% block title %}Clinet{% endblock %}

{% block body %}
    <h1>Clinet</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ clinet.id }}</td>
            </tr>
            <tr>
                <th>Username</th>
                <td>{{ clinet.username }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ clinet.nom }}</td>
            </tr>
            <tr>
                <th>Prenom</th>
                <td>{{ clinet.prenom }}</td>
            </tr>
            <tr>
                <th>Password</th>
                <td>{{ clinet.password }}</td>
            </tr>
            <tr>
                <th>Tel</th>
                <td>{{ clinet.tel }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_clinet_index') }}">back to list</a>

    <a href="{{ path('app_clinet_edit', {'id': clinet.id}) }}">edit</a>

    {{ include('clinet/_delete_form.html.twig') }}
{% endblock %}

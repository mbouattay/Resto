<form method="post" action="{{ path('app_clinet_delete', {'id': clinet.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ clinet.id) }}">
    <button class="btn">Delete</button>
</form>
